{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusUiBundle::buttons.html.twig' as buttons %}
{% import 'SyliusUiBundle::pagination.html.twig' as pagination %}
{% from 'SyliusWebBundle:Backend/Locale:macros.html.twig' import list %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.locale.index'|trans }}</a></li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_locale_create'), 'sylius.locale.create'|trans) }}
    </div>
    <h1><i class="fa fa-flag"></i> {{ 'sylius.locale.index_header'|trans|raw }}</h1>
</div>

{{ pagination.standard(locales) }}
{{ list(locales) }}
{{ pagination.standard(locales) }}

{% endblock %}
