{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% from 'SyliusUiBundle::forms.html.twig' import update %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.taxation_settings'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <h1><i class="fa fa-cog"></i> {{ 'sylius.settings.taxation_header'|trans|raw }}</h1>
</div>

{{ form_errors(form) }}

<form action="{{ path('sylius_backend_taxation_settings') }}" method="post" class="form-horizontal" novalidate>
    {{ form_row(form.default_tax_zone) }}
    {{ form_rest(form) }}
    {{ update() }}
</form>
{% endblock %}
