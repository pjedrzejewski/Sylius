{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusUiBundle::buttons.html.twig' as buttons %}
{% import 'SyliusUiBundle::pagination.html.twig' as pagination %}
{% from 'SyliusWebBundle:Backend/Country:macros.html.twig' import list %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.country.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_country_create'), 'sylius.country.create'|trans) }}
        {{ buttons.manage(path('sylius_backend_zone_index'), 'sylius.zone.manage'|trans) }}
    </div>
    <h1><i class="fa fa-flag"></i> {{ 'sylius.country.index_header'|trans|raw }}</h1>
</div>

{{ pagination.standard(countries) }}
{{ list(countries) }}
{{ pagination.standard(countries) }}

{% endblock %}
