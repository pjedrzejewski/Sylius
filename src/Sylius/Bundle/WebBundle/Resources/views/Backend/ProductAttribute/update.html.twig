{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% from 'SyliusUiBundle::forms.html.twig' import update %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.assortment'|trans }}</li>
    <li><a href="{{ path('sylius_backend_product_attribute_index') }}">{{ 'sylius.breadcrumb.attribute.index'|trans }}</a></li>
    <li>{{ product_attribute.name }}</li>
    <li>{{ 'sylius.breadcrumb.edit'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <h1><i class="fa fa-pencil"></i> {{ 'sylius.product_attribute.update_header'|trans|raw }}</h1>
</div>

{{ form_errors(form) }}

<form action="{{ path('sylius_backend_product_attribute_update', {'id': product_attribute.id}) }}" method="post" class="form-horizontal" novalidate>
    <input type="hidden" name="_method" value="PUT">
    {% include 'SyliusWebBundle:Backend/ProductAttribute:_form.html.twig' %}
    {{ update() }}
</form>
{% endblock %}
