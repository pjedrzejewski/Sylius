{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusUiBundle::buttons.html.twig' as buttons %}
{% import 'SyliusUiBundle::pagination.html.twig' as pagination %}
{% from 'SyliusWebBundle:Backend/TaxRate:macros.html.twig' import list %}

{% block breadcrumb %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.tax_rate.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_tax_rate_create'), 'sylius.tax_rate.create'|trans) }}
        {{ buttons.manage(path('sylius_backend_tax_category_index'), 'sylius.tax_category.manage'|trans) }}
    </div>
    <h1><i class="fa fa-cog"></i> {{ 'sylius.tax_rate.index_header'|trans|raw }}</h1>
</div>


{{ pagination.standard(tax_rates) }}
{{ list(tax_rates) }}
{{ pagination.standard(tax_rates) }}

{% endblock %}
